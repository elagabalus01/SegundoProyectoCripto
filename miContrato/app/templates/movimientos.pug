doctype html
html
    include constante/head.pug
    body
        h1 GobChain
        h2 Registro de información
        form("action"="/logout" "method"="POST")
            input('type'='submit','value'='logout')
